# Kredensial database asli (jangan di-commit / push ke repo publik)
config.php

# File ekspor/import (berisi data pesanan & finansial asli) — jangan di-commit
*.xlsx
*.xls
_*.csv

# OS / editor
.DS_Store
Thumbs.db
*.log

# Setelan lokal Claude Code (bisa memuat kredensial) — jangan di-commit
.claude/settings.local.json

# Folder file contoh ekspor (data asli) — jangan di-commit/deploy
contoh file/

# Skrip scratch/tes sementara
_*.php
